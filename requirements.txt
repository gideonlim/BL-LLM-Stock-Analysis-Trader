# ── Analysis Bot ──────────────────────────────────
yfinance>=0.2.30
pandas>=2.0
numpy>=1.24
pyarrow>=14.0
lxml>=4.9
tqdm>=4.60             # optional, nicer progress bars

# ── Trading Bot (shared) ─────────────────────────
alpaca-py>=0.21.0
python-dotenv>=1.0

# ── Trading Bot BL (Black-Litterman + LLM) ───────
# Core BL: uses only numpy/pandas (no extra deps)
# LLM views (optional — only needed with --llm-views):
anthropic>=0.40.0      # optional, for Claude LLM views
# openai>=1.0.0        # optional, alternative LLM provider

# ── FinBERT News Sentiment ───────────────────────
transformers>=4.30.0   # HuggingFace FinBERT model loading
torch>=2.0.0           # PyTorch inference backend

# ── Journal & PDF Reports ────────────────────────
# Core journal: no extra deps (uses stdlib json/dataclasses)
# PDF reports (optional — only needed with --report-pdf):
reportlab>=4.0         # PDF generation
matplotlib>=3.7        # chart rendering for PDF reports

# ── Day-Trader Module ────────────────────────────
# NYSE session math (holidays, half-days, DST). Used by
# day_trader/calendar.py and the EOD watchdog. See
# requirements-daytrade-live.txt for the minimal subset
# the cloud daemon installs (no torch/transformers).
pandas-market-calendars>=4.4
